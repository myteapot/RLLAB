# walk — `src/interfaces/simulation`

## Scope
- Robosuite environment wrapper, vectorized env creation, and rendering helpers.

## Validation
- Import `create_env` and `GraspEnvWrapper` after constructor changes.
