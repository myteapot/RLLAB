# walk — `src/interfaces`

## Scope
- External boundaries to LLM APIs and simulation backends.

## Layout
- `llm/` for provider-compatible text generation.
- `simulation/` for Robosuite and Gymnasium integration.

## Validation
- Import both interface packages after changing path or constructor contracts.
