# walk — `src/domain`

## Scope
- Core reward-domain contracts and safe execution rules.

## Layout
- `rewards/` defines how generated Python reward code is extracted and executed.

## Validation
- Import and execute a minimal `reward_fn` snippet after changes.
