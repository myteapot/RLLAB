# walk — `src/domain/rewards`

## Scope
- Reward code extraction, sandbox execution, and callable contract enforcement.

## Validation
- Run `safe_exec_reward` with a minimal `reward_fn` implementation.
